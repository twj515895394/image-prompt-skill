# Reference Handling Controls

当任务需要处理主辅参考、局部参考、参考强度、对象绑定、参考冲突或文字要求与图片不一致时读取本分类。

重点处理：

- 每张参考图的单一主职责
- 人物、服装、场景、构图、光影和风格绑定
- 高保留、中保留和低保留
- 同维度冲突的优先级
- 风格参考不得覆盖身份参考
- 禁止平均融合

输入图数量的基础判断仍由 `inputs/` 完成；本分类负责更复杂的参考协调方法。
