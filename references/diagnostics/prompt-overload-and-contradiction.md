# Prompt 过载与矛盾诊断

## 何时读取

Prompt 过长、重复、限制项过多、多个方向互相冲突，或模型无法抓住主目标时读取。

## 诊断顺序

1. 主体和任务目标是否被大量风格词淹没
2. 是否重复描述同一信息
3. 是否同时要求互斥结果
4. 限制项是否与当前任务无关
5. 是否把输入分析、任务规则和最终 Prompt 全部混在一起

## 修复方向

- 保留一个主目标和一条主视觉路径
- 合并重复语义
- 对互斥目标做取舍
- 删除低风险、低价值负向词
- 按主体、空间、动作、镜头、光影、材质、风格和限制重新排序
